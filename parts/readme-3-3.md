## ⚪ ️ 3.3 По возможности тестирем с реалистичным и полностью отрендеренным компонентом

✅ **Делаем:** При разумных размерах компонента, тестируем его извне, так как это делают ваши пользователи: полностью рендерим UI (Состояние), воздействуйте на него (Действите) и проверяем (Утверждение) то, что визуализированный пользовательский интерфейс ведет себя так, как ожидалось (СДУ-паттрн при тестировании).

Избегайте всевозможных моков, частичного и поверхностного (shallow) рендеринга - этот подход может привести к появлению пропущенных багов из-за недостатка деталей и усложнению поддержки (см. Пункт 1.4 «подход "черного ящика"»). Если один из дочерних компонентов значительно замедляет скорость тестов (например, анимацией) или доставляет сложности в настройке - попробуйте явно заменить его на фальшивый.

Со всем вышесказанным нужно соблюдать осторожность: этот метод работает для малых / средних компонентов, которые содержат не слишком много дочерних компонентов.

Полный рендеринг компонента со слишком большим количеством дочерних элементов затруднит выявление источника бага и может быть слишком медленным. В таких случаях напишем только несколько тестов для этого компонента и больше тестов для его дочерних компонентов.

<br/>

❌ **Иначе:** Когда мы компаемся во внутренностях компонента, вызывая его приватные методы и проверяем внутреннее состояние - нам придется реорганизовать все тесты при рефакторинге реализации компонентов. У вашей команды действительно есть ёмкость чтобы поддерживать компоненты таким образом?

<br/>

<details><summary>✏ <b>Пример кода</b></summary>

<br/>

### 👏 Правильно: Реалистичное поведение с полностью отрендеренным компонентом

![](https://img.shields.io/badge/🔧%20Example%20using%20React-blue.svg "Examples with React") ![](https://img.shields.io/badge/🔧%20Example%20using%20Enzyme-blue.svg "Examples with Enzyme")

```javascript
class Calendar extends React.Component {
  static defaultProps = { showFilters: false };

  render() {
    return (
      <div>
        A filters panel with a button to hide/show filters
        <FiltersPanel showFilter={showFilters} title="Choose Filters" />
      </div>
    );
  }
}

//В примере используем React & Enzyme
test("Реалистичный подход: когда кликаем на показать фильтры — они отображаются", () => {
  // Состояние
  const wrapper = mount(<Calendar showFilters={false} />);

  // Действие
  wrapper.find("button").simulate("click");

  // Утверждение
  expect(wrapper.text().includes("Choose Filter"));
  // это то как юзер увидит наши фильтры: по тексту
});
```

### 👎 Неправильно: "Мокаем реальность" поверхностным рендерингом

```javascript
test("Поверхностный/моковый подход: когда кликаем на показать фильтры — они отображаются", () => {
  // Состояние
  const wrapper = shallow(
    <Calendar showFilters={false} title="Choose Filter" />
  );

  // Действие
  wrapper
    .find("filtersPanel")
    .instance()
    .showFilters();
  // Дёргаем внутренние методы, обходим UI и вызываем showFilters напрямую. Подход "белого ящика"

  // Утверждение
  expect(wrapper.find("Filter").props()).toEqual({ title: "Choose Filter" });
  // что если мы изменили имя свойства или не передали ничего релевантного?
});
```

</details>

<br/>
